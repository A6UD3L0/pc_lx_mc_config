# =============================================================================
# Portable Development Environment (PDE) - Environment Configuration
# Copy this file to .env and customize as needed
# =============================================================================

# -----------------------------------------------------------------------------
# User Identity (UID/GID Mapping)
# -----------------------------------------------------------------------------
# Set these to match your host user to prevent permission issues on mounted volumes
# Run `id -u` and `id -g` on your host to get these values
HOST_UID=1000
HOST_GID=1000

# -----------------------------------------------------------------------------
# SSH Configuration
# -----------------------------------------------------------------------------
# SSH agent socket path on the host
# - macOS (Docker Desktop): /run/host-services/ssh-auth.sock
# - Linux: Usually $SSH_AUTH_SOCK (e.g., /run/user/1000/keyring/ssh)
# - Windows WSL2: /mnt/wsl/ssh-agent.sock (if using wsl-ssh-agent)
SSH_AUTH_SOCK_HOST=/run/host-services/ssh-auth.sock

# Port for SSH access to the container (for WezTerm multiplexing, remote dev)
SSH_PORT=2222

# Start SSH server on container startup
START_SSHD=true

# -----------------------------------------------------------------------------
# Dotfiles Configuration
# -----------------------------------------------------------------------------
# Git repository URL for your Neovim dotfiles (optional)
# Supports SSH URLs (requires SSH agent) or HTTPS URLs
# Example: git@github.com:username/dotfiles.git
# Example: https://github.com/username/dotfiles.git
DOTFILES_REPO=

# -----------------------------------------------------------------------------
# Jupyter Configuration
# -----------------------------------------------------------------------------
# Start Jupyter Lab on container startup
START_JUPYTER=false

# Port for Jupyter Lab
JUPYTER_PORT=8888

# -----------------------------------------------------------------------------
# ML/Development Ports
# -----------------------------------------------------------------------------
# TensorBoard
TENSORBOARD_PORT=6006

# MLflow tracking server
MLFLOW_PORT=5050

# Kedro Viz
KEDRO_VIZ_PORT=4141

# -----------------------------------------------------------------------------
# Mosh Configuration (Mobile Shell)
# -----------------------------------------------------------------------------
# NOTE: Mosh does NOT support terminal graphics (Sixel/Kitty protocols)
# Use SSH for image-heavy workflows (molten-nvim, image.nvim)
# Use Mosh for text-only mobile access with high latency tolerance
# Mosh UDP ports 60000-60005 are exposed by default

# -----------------------------------------------------------------------------
# NVIDIA GPU Configuration
# -----------------------------------------------------------------------------
# Which GPUs to expose to the container
# Options: all, none, 0, 1, 0,1, GPU-UUID
NVIDIA_VISIBLE_DEVICES=all

# GPU capabilities to enable
# Options: compute, utility, graphics, video, display
NVIDIA_DRIVER_CAPABILITIES=compute,utility

# -----------------------------------------------------------------------------
# Python Configuration
# -----------------------------------------------------------------------------
# Disable .pyc file generation
PYTHONDONTWRITEBYTECODE=1

# Unbuffered output (useful for logging)
PYTHONUNBUFFERED=1

# -----------------------------------------------------------------------------
# Terminal Configuration
# -----------------------------------------------------------------------------
TERM=xterm-256color
COLORTERM=truecolor
HOST_PROJECTS_PATH=/Users/jf/Desktop/GIT/s1
